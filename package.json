{
	"private": true,
	"scripts": {
		"minify-svg": "svgo --config=.svgo.config.js --quiet --recursive --folder icons/ && svgo --config=.svgo.config.js --quiet --recursive --folder logos/"
	},
	"devDependencies": {
		"svgo": "4.0.0"
	}
}